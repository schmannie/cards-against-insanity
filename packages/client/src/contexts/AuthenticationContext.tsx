import { createContext } from 'react';

// function getAuthentationState(player: Player = null) {
//   return {
//     token: player ? player.token : null,
//     displayName: player ? player.displayName : null,
//   };
// }
